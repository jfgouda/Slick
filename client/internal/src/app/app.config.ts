import { environment } from "environments/environment";


export class AppConfig {
    //public readonly apiUrl = `http://${environment.production ? 'localhost' : 'localhost'}/sketch-server/api/v1`;
    public readonly apiUrl = `https://localhost/sketch-server/api/v1`;
    public readonly idbBCA = 'ngr-bca-idb';
};

//atl-l-35ldqf2